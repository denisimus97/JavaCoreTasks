package com.company.fille.manager;

public class FileWorker {
    public static void main(String[] args) {

        FileManager example = new FileManager();
        boolean worker = true ;
        while (worker )
        {
            worker = example.userComand();
        }

    }
}
